<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="year"
            type="String" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <View
            android:id="@+id/placeholder_view_line"
            android:layout_width="1dp"
            android:layout_height="0dp"
            android:background="@color/colorPrimaryDark"
            app:layout_constraintHorizontal_bias="0.0"
            android:layout_marginStart="10dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <ImageView
            android:id="@+id/placeholder_image_bullet_point"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/imcd_bullet_point"
            android:src="@drawable/ic_circle_color_primary_dark"
            android:layout_marginTop="20dp"
            app:layout_constraintBottom_toTopOf="@id/list_historic_events"
            app:layout_constraintStart_toStartOf="@id/placeholder_view_line"
            app:layout_constraintEnd_toEndOf="@id/placeholder_view_line"
            app:layout_constraintTop_toTopOf="parent"/>

        <TextView
            android:id="@+id/value_text_year"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:textColor="@color/color_text"
            android:textSize="20sp"
            android:fontFamily="@font/roboto"
            android:text="@{year}"
            app:layout_constraintHorizontal_bias="0"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/placeholder_view_line"
            app:layout_constraintTop_toTopOf="@id/placeholder_image_bullet_point"
            app:layout_constraintBottom_toBottomOf="@id/placeholder_image_bullet_point"
            tools:text="20XX"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/list_historic_events"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="10dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_historic_event"
            tools:itemCount="2"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/placeholder_image_bullet_point"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
